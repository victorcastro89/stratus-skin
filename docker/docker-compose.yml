services:
  # ---------- Local mail server (IMAP + SMTP) ----------
  mailserver:
    image: ghcr.io/docker-mailserver/docker-mailserver:latest
    container_name: mailserver
    hostname: mail.example.test
    domainname: example.test
    ports:
      - "3025:25"    # SMTP
      - "3143:143"   # IMAP
      - "3587:587"   # Submission
    volumes:
      - maildata:/var/mail
      - mailstate:/var/mail-state
      - maillogs:/var/log/mail
      - ./docker-data/dms/config/:/tmp/docker-mailserver/
    environment:
      # Disable heavy services not needed for dev
      - ENABLE_SPAMASSASSIN=0
      - ENABLE_CLAMAV=0
      - ENABLE_FAIL2BAN=0
      - ENABLE_POSTGREY=0
      - ENABLE_AMAVIS=0
      - ENABLE_UPDATE_CHECK=0
      # No SSL for local dev (plain IMAP/SMTP)
      - SSL_TYPE=
      # Allow relay from any container on the same docker network
      - PERMIT_DOCKER=connected-networks
      - OVERRIDE_HOSTNAME=mail.example.test
      - POSTFIX_INET_PROTOCOLS=ipv4
      - DOVECOT_INET_PROTOCOLS=ipv4
      # Accept mail without strict hostname checks
      - ENABLE_SRS=0
    restart: unless-stopped
    healthcheck:
      test: ss --listening --tcp | grep -P 'LISTEN.+:smtp' || exit 1
      interval: 15s
      timeout: 5s
      retries: 20
      start_period: 60s

  # ---------- Roundcube webmail ----------
  roundcube:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: roundcube-dev
    ports:
      - "8000:80"
    volumes:
      # Roundcube core (cloned by setup.sh)
      - ../roundcubemail:/var/www/html:delegated
      # Mount our custom skins/plugins on top (bind mount beats symlinks)
      - ../skins/stratus:/var/www/html/skins/stratus:delegated
      # Mount config
      - ../roundcubemail/config/config.inc.php:/var/www/html/config/config.inc.php:delegated
    depends_on:
      mailserver:
        condition: service_healthy
    environment:
      - PHP_IDE_CONFIG=serverName=roundcube-docker

volumes:
  maildata:
  mailstate:
  maillogs:
